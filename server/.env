# Database configuration
DB_HOST=localhost
DB_PORT=5432
DB_DATABASE=sports_event_booking
DB_USER=postgres
DB_PASSWORD=postgres

# Secret key for JWT (if you're using JWT for authentication)
JWT_SECRET=your_secret_key

# Port for the Node.js server
PORT=3001

# CORS configuration (if you need to specify allowed origins)
CORS_ORIGIN=http://localhost:3000
